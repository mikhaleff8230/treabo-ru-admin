# --- Локально (Vite proxy /api -> см. vite.config.js) ---
# VITE_API_BASE=
# VITE_ADMIN_TOKEN=admin

# --- Прод: отдельный поддомен админки, API на основном сайте ---
# VITE_API_BASE=https://proffi.sancan.ru
#
# ВНИМАНИЕ: любое значение VITE_* попадает в собранный JS. Не кладите сюда
# реальный ADMIN_TOKEN — вводите токен только в форме входа в админке.


# Copy to .env.production on VPS: cp env.vps.template .env.production

# Browser calls API via shop/nginx host (same IP until DNS)
NEXT_PUBLIC_REST_API_ENDPOINT=http://13.140.165.171/api
NEXT_PUBLIC_API_URL=http://13.140.165.171/api

# Do NOT commit real token — enter on login screen or set only on server
NEXT_PUBLIC_PROFFI_ADMIN_TOKEN=

NEXT_PUBLIC_DEFAULT_LANGUAGE=ru
NEXT_PUBLIC_ENABLE_MULTI_LANG=false
NEXT_PUBLIC_AVAILABLE_LANGUAGES=ru

APPLICATION_MODE=production
